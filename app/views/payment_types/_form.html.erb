<%= form_with(model: payment_type, local: true) do |form| %>
  <% if payment_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(payment_type.errors.count, "error") %> prohibited this payment_type from being saved:</h2>

      <ul>
      <% payment_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :payment_type %>
    <%= form.text_area :payment_type, id: :payment_type_payment_type %>
  </div>

  <div class="field">
    <%= form.label :account_number %>
    <%= form.text_area :account_number, id: :payment_type_account_number %>
  </div>

  <div class="field">
    <%= form.label :company_id %>
    <%= form.text_field :company_id, id: :payment_type_company_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
